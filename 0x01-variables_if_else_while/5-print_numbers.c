#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int m;

	for (m = 0; m < 10; m++)
	printf("%d", m);
	printf("\n");

	return (0);
}
